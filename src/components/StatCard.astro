---
export interface Props {
  label: string;
  value: string;
  note?: string;
}

const { label, value, note } = Astro.props;
---

<article class="stat-card" box-="square">
  <span>{label}</span>
  <strong>{value}</strong>
  {note && <small>{note}</small>}
</article>
